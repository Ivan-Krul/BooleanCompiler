function xor (a, b) {
	end ((a and not b) or (not a and b)); // round brackets are more comfortable use the operators
}

function main() {

	init a[4]; //masive

	g = a[1];//adress is initialized as cell in massive

	cycle(i in a) { //like foreach
		out < i < nline; // new line
	}
		
	g = g and true; // is same like
	//a[1] = a[1] and true;

	end;
}